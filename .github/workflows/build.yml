name: build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - name: Build and test
        if: runner.os != 'Windows'
        run: ./code/scripts/build.sh --gurobi off
      - name: Build and test
        if: runner.os == 'Windows'
        shell: cmd
        run: call code\scripts\build.bat --gurobi off
      - name: Test run launchers
        if: runner.os != 'Windows'
        run: |
          ./code/scripts/run_examples.sh --output-dir build/launcher-smoke
          ./code/scripts/run_examples.sh --output-dir build/launcher-smoke
          mkdir -p "build/relative launcher"
          cp data/instances/otto/n_0020/instance_n=20_1.txt \
            "build/relative launcher/local-instance.txt"
          test -f "build/relative launcher/local-instance.txt"
          (
            cd "build/relative launcher"
            ../../code/scripts/run_batch.sh \
              --problem bpp-p \
              --input local-instance.txt \
              --time-limit 10 \
              --output-dir "relative output"
          )
          ./code/scripts/run_parallel.sh \
            --check-only \
            --output-dir "build/parallel launcher (smoke)&test"
      - name: Test run launchers
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          call code\scripts\run_examples.bat --output-dir build\launcher-smoke
          if errorlevel 1 exit /b 1
          call code\scripts\run_examples.bat --output-dir build\launcher-smoke
          if errorlevel 1 exit /b 1
          mkdir "build\relative launcher"
          copy /Y data\instances\otto\n_0020\instance_n=20_1.txt "build\relative launcher\local-instance.txt" >nul
          if not exist "build\relative launcher\local-instance.txt" exit /b 1
          pushd "build\relative launcher"
          if errorlevel 1 exit /b 1
          if not exist local-instance.txt exit /b 1
          call ..\..\code\scripts\run_batch.bat --problem bpp-p --input local-instance.txt --time-limit 10 --output-dir "relative output"
          if errorlevel 1 exit /b 1
          popd
          call code\scripts\run_parallel.bat --check-only --output-dir "build\parallel launcher (smoke)&test"
          if errorlevel 1 exit /b 1
